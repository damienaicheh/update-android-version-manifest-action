{
  "name": "update-android-version-manifest-action",
  "version": "2.0.0",
  "description": "This action update the `versionCode` and `versionName` properties of the AndroidManifest.xml file for your Android projects.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package-action": "tsc && ncc build lib/index.js --license licenses.txt"
  },
  "author": "damienaicheh",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "fs": "0.0.1-security"
  },
  "devDependencies": {
    "@types/node": "^16.0.0",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.2.2"
  }
}
